{
  "name": "dasic-atlas-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "DASIC ERP — FastAPI backend + Vite SPA (en /web/).",
  "scripts": {
    "build": "cd web && npm ci && npm run build",
    "typecheck": "cd web && npm run typecheck",
    "dev:web": "cd web && npm run dev"
  }
}
